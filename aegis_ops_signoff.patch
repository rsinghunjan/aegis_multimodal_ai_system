*** Begin Patch
*** Add File: scripts/ops_finalize_and_report.sh
+#!/usr/bin/env bash
+#
+# Orchestrator to run final operational validation steps and produce a sign-off JSON report.
+# This script runs:
+#  - E2E artifact proof (scripts/e2e_run_and_verify.sh)
+#  - Gatekeeper remediation PR generation/apply (scripts/auto_apply_gatekeeper_prs.sh)
+#  - Secrets inventory & dry-run rotation (scripts/rotate_flagged_secrets_dryrun.sh)
+#  - Create short-lived Vault CI token (scripts/create_vault_ci_token.sh)
+#  - Apply Vault policy (scripts/vault_apply_and_rotate.sh)
+#  - Deploy governance OIDC ingress (scripts/deploy_governance_oidc.sh)
+#  - Test governance approval flow (scripts/test_governance_approve.sh)
+#  - CI policy enforcement quick-check (scripts/check_pr_pinning_sbom.py)
+#  - Observability quick checks (monitoring/verify_alerts.sh)
+#  - DR rehearsal (scripts/dr_rehearsal_runner.sh)
+#
+# Output:
+#  - signoff_report.json in current directory with step results and paths to logs.
+set -euo pipefail
+
+WORKDIR="$(pwd)/ops_signoff_$(date -u +%Y%m%dT%H%M%SZ)"
+mkdir -p "$WORKDIR/logs"
+REPORT="$WORKDIR/signoff_report.json"
+declare -A STATUS
+declare -A MSG
+declare -A LOG
+
+run_step() {
+  local name="$1"; shift
+  local cmd=( "$@" )
+  local logfile="$WORKDIR/logs/${name}.log"
+  echo "=== STEP: $name ===" | tee "$logfile"
+  set +e
+  "${cmd[@]}" >>"$logfile" 2>&1
+  rc=$?
+  set -e
+  if [ $rc -eq 0 ]; then
+    STATUS["$name"]="success"
+    MSG["$name"]="ok"
+  else
+    STATUS["$name"]="failure"
+    MSG["$name"]="failed (exit $rc) - see log"
+  fi
+  LOG["$name"]="$logfile"
+  echo "=== END STEP: $name (rc=$rc) ===" >>"$logfile"
+  return $rc
+}
+
+echo "Starting Aegis ops finalization run; working dir: $WORKDIR"
+
+# 1) E2E artifact proof
+WF_YAML="${1:-}"  # optional first argument: path to argo workflow yaml to submit
+if [ -n "$WF_YAML" ] && [ -f "$WF_YAML" ]; then
+  run_step "e2e_artifact_proof" ./scripts/e2e_run_and_verify.sh "$WF_YAML"
+else
+  # if no workflow provided, attempt to run without submitting (will inspect latest)
+  run_step "e2e_artifact_proof" ./scripts/e2e_run_and_verify.sh ""
+fi
+
+# 2) Gatekeeper remediation -> generate PRs (dry-run)
+run_step "gatekeeper_prs_dryrun" ./scripts/auto_apply_gatekeeper_prs.sh dry-run
+
+# Optionally apply PRs (CAUTION: creates branches & PRs). Controlled by env APPLY_GATEKEEPER=true
+if [ "${APPLY_GATEKEEPER:-false}" = "true" ]; then
+  run_step "gatekeeper_prs_apply" ./scripts/auto_apply_gatekeeper_prs.sh apply || true
+fi
+
+# 3) Secrets inventory & dry-run rotate
+GITHUB_REPO="${GITHUB_REPO:-}"
+if [ -n "$GITHUB_REPO" ]; then
+  run_step "secrets_inventory" python3 scripts/secret_inventory_improve.py --repo](#)


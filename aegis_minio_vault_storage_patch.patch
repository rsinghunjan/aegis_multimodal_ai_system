*** Begin Patch
*** Add File: infra/k8s/storage/local-path-storageclass.yaml
+apiVersion: storage.k8s.io/v1
+kind: StorageClass
+metadata:
+  name: local-path
+provisioner: rancher.io/local-path
+reclaimPolicy: Delete
+volumeBindingMode: WaitForFirstConsumer
+mountOptions:
+  - "rw"
+
+# Notes:
+# - This StorageClass uses the local-path provisioner (rancher/local-path-provisioner).
+# - Install the provisioner with the provided script scripts/install_local_path_provisioner.sh
+
*** End Patch
*** Begin Patch
*** Add File: scripts/install_local_path_provisioner.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# install_local_path_provisioner.sh
+# Install the rancher/local-path-provisioner (useful for dev/staging clusters that lack dynamic storage).
+#
+# Usage:
+#   ./scripts/install_local_path_provisioner.sh
+#
+helm repo add local-path https://rancher.github.io/local-path-provisioner || true
+helm repo update
+kubectl create namespace local-path-storage --dry-run=client -o yaml | kubectl apply -f -
+helm upgrade --install local-path-provisioner local-path/local-path-provisioner -n local-path-storage
+echo "Local-path provisioner installed. Apply infra/k8s/storage/local-path-storageclass.yaml if needed."
+
*** End Patch
*** Begin Patch
*** Add File: infra/k8s/pv-pvc/minio-pvc.yaml
+apiVersion: v1
+kind: PersistentVolumeClaim
+metadata:
+  name: minio-data-pvc
+  namespace: aegis
+spec:
+  accessModes:
+    - ReadWriteOnce
+  resources:
+    requests:
+      storage: 100Gi
+  storageClassName: local-path
+
+---
+apiVersion: v1
+kind: PersistentVolumeClaim
+metadata:
+  name: vault-data-pvc
+  namespace: aegis
+spec



# Append these runtime dependencies if you intend to use Vault and OIDC/JWKS verification:
hvac==1.4.0
python-jose[cryptography]==3.3.0
cryptography==40.0.0
boto3==1.28.0  # if you use AWS Secrets Manager for rotation/storage

*** Begin Patch
*** Add File: scripts/ops_run_operational_validation.sh
+#!/usr/bin/env bash
+#
+# Orchestrate a set of operational validation checks to reduce gaps identified in the platform.
+# This script runs non-destructive validation steps:
+#  - E2E artifact proof & Rekor inclusion
+#  - Gatekeeper pilot deny smoke tests (dry-run mode by default)
+#  - Secrets rotation dry-run / minimal rotation helper (requires Vault)
+#  - Alerting webhook smoke test (sends test alert to Alertmanager or receives via webhook receiver)
+#  - DCGM / GPU metric presence check (Prometheus)
+#  - Feast backup/restore smoke test (optional)
+#  - Katib trial artifact annotation scan
+#  - SHAP upload + retrieval check (optional)
+#
+# Usage:
+#   ./scripts/ops_run_operational_validation.sh [--apply-gatekeeper] [--feast-test] [--shap-test]
+set -euo pipefail
+
+SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
+
+PRINT_HEADER(){ echo; echo "===== $1 ====="; }
+
+# ENV pre-reqs
+PROM_URL="${PROM_URL:-http://prometheus.monitoring.svc.cluster.local:9090}"
+ALERTMGR_URL="${ALERTMGR_URL:-http://alertmanager.monitoring.svc.cluster.local:9093}"
+OBJECT_STORE_BUCKET="${OBJECT_STORE_BUCKET:-}"
+METRICS_BUCKET="${METRICS_BUCKET:-}"
+GOV_URL="${GOV_URL:-}"
+GOV_API_KEY="${GOV_API_KEY:-}"
+VAULT_PATH="${VAULT_PATH:-secret/data/aegis/ci}"
+APPLY_GATEKEEPER=false
+FEAST_TEST=false
+SHAP_TEST=false
+
+while [[ $# -gt 0 ]]; do
+  case "$1" in
+    --apply-gatekeeper) APPLY_GATEKEEPER=true; shift ;;
+    --feast-test) FEAST_TEST=true; shift ;;
+    --shap-test) SHAP_TEST=true; shift ;;
+    *) echo "Unknown arg: $1"; exit 2 ;;
+  esac
+done
+
+failures=0
+
+########## 1) E2E artifact proof + Rekor inclusion ##########
+PRINT_HEADER "E2E artifact proof (cosign + Rekor) - running ./scripts/e2e_run_and_verify.sh"
+if [ -x "${SCRIPT_DIR}/e2e_run_and_verify.sh" ]; then
+  set +e
+  "${SCRIPT_DIR}/e2e_run_and_verify.sh" "" > /tmp/e2e_run.log 2>&1
+  rc=$?
+  set -e
+  if [ $rc -eq 0 ]; then
+    echo "E2E script succeeded. Log: /tmp/e2e_run.log"
+  else
+    echo "E2E script failed (rc=$rc). See /tmp/e2e_run.log"
+    failures=$((failures+1))
+  fi
+else
+  echo "Missing e2e runner script at ${SCRIPT_DIR}/e2e_run_and_verify.sh; skip"
+fi
+
+# Try to find attestation and check Rekor inclusion if present
+if [ -n "$OBJECT_STORE_BUCKET" ]; then
+  echo "Looking for recent attestation in s3://${OBJECT_STORE_BUCKET}/model-archives/"
+  ART_KEY=$(aws s3 ls "s3://${OBJECT_STORE_BUCKET}/model-archives/" --recursive | sort | tail -n1 | awk '{print $4}' || true)
+  if [ -n "$ART_KEY" ]; then
+    ATTEST=$(](#)*


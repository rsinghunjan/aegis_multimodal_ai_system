*** Begin Patch
*** Update File: scripts/storage_cp.sh
@@
   "gcp")
     case "$cmd" in
       cp) gsutil cp -r "$src" "$dst" ;;
       sync) gsutil -m rsync -r "$src" "$dst" ;;
       *) echo "Unsupported cmd $cmd for gcp" >&2; exit 3 ;;
     esac
     ;;
+  "aliyun"|"alibaba"|"oss")
+    # Alibaba Cloud OSS support
+    # Preferred: use ossutil (https://help.aliyun.com/document_detail/32008.html)
+    # Fallback: use aliyun cli `aliyun oss` if ossutil is not available.
+    case "$cmd" in
+      cp)
+        if command -v ossutil >/dev/null 2>&1; then
+          # dst example: oss://<bucket>/<path>
+          ossutil cp "$src" "$dst" --recursive
+        elif command -v aliyun >/dev/null 2>&1; then
+          # aliyun cli: aliyun oss cp --recursive <src> <dst>
+          aliyun oss cp --recursive "$src" "$dst"
+        else
+          echo "ossutil or aliyun CLI not found. Install ossutil (recommended) or aliyun CLI." >&2
+          exit 3
+        fi
+        ;;
+      sync)
+        if command -v ossutil >/dev/null 2>&1; then
+          # ossutil sync is handy; use cp -r as fallback
+          ossutil cp "$src" "$dst" --recursive
+        else
+          echo "Sync fallback using recursive cp (ossutil recommended for real sync)" >&2
+          if command -v aliyun >/dev/null 2>&1; then
+            aliyun oss cp --recursive "$src" "$dst"
+          else
+            echo "ossutil or aliyun CLI not found. Install ossutil or aliyun CLI." >&2
+            exit 3
+          fi
+        fi
+        ;;
+      *)
+        echo "Unsupported cmd $cmd for aliyun/oss" >&2; exit 3 ;;
+    esac
+    ;;
*** End Patch
*** Begin Patch
*** Update File: scripts/push_image_crosscloud.sh
@@
   "oci")
@@
     docker build -t "${FULL}" -f "${DOCKERFILE}" .
     docker push "${FULL}"
     echo "${FULL}"
     ;;
+  "aliyun"|"alibaba")
+    # Alibaba Container Registry (ACR) style push.
+    # Expected env:
+    #   ALIYUN_REGISTRY_HOST (e.g., registry.cn-shanghai.aliyuncs.com)
+   


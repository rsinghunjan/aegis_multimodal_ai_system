*** Begin Patch
*** Add File: .github/workflows/milvus_prod_ha_deploy.yml
+name: Milvus Production HA Deploy & Verification
+
+on:
+  workflow_dispatch:
+
+jobs:
+  deploy-verify:
+    runs-on: ubuntu-latest
+    env:
+      NAMESPACE: "aegis"
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup kubectl & helm
+        run: |
+          sudo apt-get update -y
+          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
+          chmod +x kubectl && sudo mv kubectl /usr/local/bin/
+          curl -fsSL https://get.helm.sh/helm-v3.12.0-linux-amd64.tar.gz | tar xz
+          sudo mv linux-amd64/helm /usr/local/bin/helm
+
+      - name: Configure kubeconfig
+        env:
+          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
+        run: |
+          echo "$KUBE_CONFIG_DATA" | base64 --decode > /tmp/kubeconfig
+          export KUBECONFIG=/tmp/kubeconfig
+          kubectl version --client
+
+      - name: Deploy Milvus with prod values
+        env:
+          MILVUS_VALUES_BASE64: ${{ secrets.MILVUS_VALUES_BASE64 }}
+        run: |
+          helm repo add milvus https://milvus-io.github.io/milvus-helm/ || true
+          helm repo update
+          mkdir -p /tmp/helmvals
+          if [ -n "$MILVUS_VALUES_BASE64" ]; then
+            echo "$MILVUS_VALUES_BASE64" | base64 --decode > /tmp/helmvals/milvus-values.yaml
+            helm upgrade --install aegis-milvus milvus/milvus -n ${NAMESPACE} -f /tmp/helmvals/milvus-values.yaml --create-namespace
+          else
+            helm upgrade --install aegis-milvus milvus/milvus -n ${NAMESPACE} --create-namespace
+          fi
+
+      - name: Wait for Milvus pods ready
+        run: |
+          kubectl -n ${NAMESPACE} wait --for=condition=ready pods --all --timeout=15m
+
+      - name: Verify TLS & Auth secrets exist
+        run: |
+          kubectl -n ${NAMESPACE} get secret aegis-milvus-tls || (echo "TLS secret missing" && exit 1)
+          kubectl -n ${NAMESPACE} get secret aegis-milvus-root || (echo "Root auth secret missing" && exit 1)
+
+      - name: Ensure ServiceMonitor / PrometheusRule present (monitoring namespace)
+        run: |
+          kubectl -n monitoring get servicemonitor --ignore-not-found | grep milvus || echo "ServiceMonitor milvus might be missing"
+          kubectl -n monitoring get prometheusrule --ignore-not-found | grep milvus || echo "PrometheusRule milvus might be missing"
+
+      - name: Trigger one-off backup job (if CronJob exists)
+        env:
+          GCS_BUCKET: ${{ secrets.MILVUS_BACKUP_GCS_BUCKET }}
+          S3_BUCKET: ${{ secrets.MILVUS_BACKUP_S3_BUCKET }}
+        run: |
+          CRON=$(kubectl -n ${NAMESPACE} get cronjob -o jsonpath='{.items[?(@.metadata.name=="milvus-backup")].metadata.name}' || true)
+          if [ -n "$CRON" ]; then
+            JOB_JSON=$(kubectl -n ${NAMESPACE} create job --from=cronjob/"$CRON" "manual-milvus-backup-$(date +%s)" -o json)
+            JOB_NAME=$(echo "$JOB_JSON" | jq -r .metadata.name)
+            kubectl -n ${NAMESPACE} wait --for=condition=complete job/"$JOB_NAME" --timeout=6m || echo "Backup job may not have completed"
+            kubectl -n ${NAMESPACE} logs job/"$JOB_NAME" || true
+            if [ -n "$GCS_BUCKET" ] && command -v gsutil >/dev/null 2>&1; then
+              gsutil ls "$GCS_BUCKET" || echo "No objects in GCS bucket or permission denied"
+            fi
+            if [ -n "$S3_BUCKET" ] && command -v aws >/dev/null 2>&1; then
+              aws s3 ls](#)
ae


# TensorFlow & tooling (pick CPU or GPU variant in your deployment images)
tensorflow==2.14.1
mlflow==2.6.2

# Optional optimization & serving tools
tensorflow-addons==0.21.0

# Utilities
numpy==1.26.4
pyyaml==6.0
prometheus-client==0.17.0
protobuf==4.24.6

# Optional (only install if you will use TF-TRT or OCI/other adapters)
# For TF-TRT conversion on CI you will need a GPU runner with CUDA and the matching TF binary.

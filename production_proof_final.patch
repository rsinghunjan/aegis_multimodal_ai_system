*** Begin Patch
*** Add File: .github/workflows/production_proof_gate.yml
+name: Production Proof, Tuning & Governance Gate
+
+on:
+  workflow_dispatch:
+  # Optional scheduled daily runs during validation window
+  schedule:
+    - cron: '0 6 * * *'
+
+jobs:
+  proof-gate:
+    runs-on: ubuntu-latest
+    timeout-minutes: 180
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup Python
+        uses: actions/setup-python@v4
+        with:
+          python-version: "3.10"
+
+      - name: Install system tools
+        run: |
+          sudo apt-get update -y
+          sudo apt-get install -y jq
+          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
+          chmod +x kubectl && sudo mv kubectl /usr/local/bin/
+          curl -fsSL https://get.helm.sh/helm-v3.12.0-linux-amd64.tar.gz | tar xz
+          sudo mv linux-amd64/helm /usr/local/bin/helm
+
+      - name: Configure kubeconfig
+        env:
+          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
+        run: |
+          if [ -z "${KUBE_CONFIG_DATA:-}" ]; then
+            echo "KUBE_CONFIG_DATA missing; abort."
+            exit 2
+          fi
+          echo "$KUBE_CONFIG_DATA" | base64 --decode > /tmp/kubeconfig
+          export KUBECONFIG=/tmp/kubeconfig
+          kubectl cluster-info || true
+        shell: bash
+
+      - name: Install Python deps
+        run: |
+          python -m pip install --upgrade pip
+          pip install -r requirements.txt || true
+          pip install requests pymilvus prometheus-api-client python-dateutil PyYAML || true
+
+      - name: Run Fully AI Readiness Gate (milvus/load/gatekeeper/operator/audit/kpi)
+        env:
+          LLM_GATEWAY_URL: ${{ secrets.LLM_GATEWAY_URL }}
+          LLM_GATEWAY_API_KEY: ${{ secrets.LLM_GATEWAY_API_KEY }}
+          MILVUS_HOST: ${{ secrets.MILVUS_HOST }}
+          MILVUS_PORT: ${{ secrets.MILVUS_PORT }}
+          AUDIT_GCS_BUCKET: ${{ secrets.AUDIT_GCS_BUCKET }}
+          AUDIT_S3_BUCKET: ${{ secrets.AUDIT_S3_BUCKET }}
+          PROM_URL: ${{ secrets.PROM_URL }}
+          PROM_TOKEN: ${{ secrets.PROM_TOKEN }}
+          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
+          MILVUS_VALUES_BASE64: ${{ secrets.MILVUS_VALUES_BASE64 }}
+        run: |
+          set -euo pipefail
+          # Reuse existing readiness gate workflow script from repo
+          echo "Running readiness gate..."
+          gh workflow run fully_ai_readiness_gate.yml --repo ${{ github.repository }} || true
+          # Also run operator e2e repeat collector directly to surface immediate failures
+          python scripts/operator_e2e_repeat.py || { echo "operator_e2e_repeat failed"; exit 2; }
+        shell: bash
+
+      - name: Run chaos & failure simulations
+        env:
+          KUBE_CONFIG](#)*


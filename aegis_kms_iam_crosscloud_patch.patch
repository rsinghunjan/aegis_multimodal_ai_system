*** Begin Patch
*** Add File: infra/helm/milvus/templates/backup-job-kms.yaml
+{{- /*
+  Backup Job template for Milvus that supports provider-specific KMS/KeyVault wiring.
+  This is a Helm template fragment intended to be included as a Job hook or CronJob.
+  Values referenced:
+    values.backup.provider: "gcp" | "aws" | "azure"
+    values.backup.* provider-specific fields (s3.bucket, gcs.bucket, azure.storageAccount/container)
+    values.backup.encryption.* provider-specific KMS settings
+    values.serviceAccount.name (optional) - KSA name to use for the job
+*/ -}}
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: {{ include "milvus.fullname" . }}-backup
+  labels:
+    app.kubernetes.io/name: {{ include "milvus.name" . }}
+    app.kubernetes.io/instance: {{ .Release.Name }}
+spec:
+  template:
+    metadata:
+      labels:
+        app.kubernetes.io/name: {{ include "milvus.name" . }}
+        app.kubernetes.io/instance: {{ .Release.Name }}
+    spec:
+      {{- if .Values.serviceAccount.name }}
+      serviceAccountName: {{ .Values.serviceAccount.name }}
+      {{- end }}
+      restartPolicy: Never
+      containers:
+        - name: milvus-backup
+          image: {{ .Values.backup.image | default "alpine:3.18" }}
+          command: ["/bin/sh", "-c"]
+          args:
+            - |
+              set -euo pipefail
+              echo "Starting milvus backup for provider={{ .Values.backup.provider }}"
+{{- if eq .Values.backup.provider "aws" }}
+              # AWS S3 + KMS settings
+              export S3_BUCKET="{{ .Values.backup.s3.bucket }}"
+              export AWS_REGION="{{ .Values.backup.s3.region }}"
+              export KMS_KEY_ID="{{ .Values.backup.encryption.kms


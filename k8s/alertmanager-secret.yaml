  1
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-secrets
  namespace: monitoring
type: Opaque
stringData:
  # These values should be injected via CI/CD or created with `kubectl create secret` out-of-band.
  # Do NOT commit real secrets in the repository.
  SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/REPLACE_ME"
  SAFETY_SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/REPLACE_ME"
  PAGERDUTY_ROUTING_KEY: "REPLACE_ME"

*** Begin Patch
*** Add File: k8s/gatekeeper_constrainttemplate_suggestedaction.yaml
+apiVersion: templates.gatekeeper.sh/v1beta1
+kind: ConstraintTemplate
+metadata:
+  name: aegissuggestedactionpolicy
+spec:
+  crd:
+    spec:
+      names:
+        kind: AegisSuggestedActionPolicy
+  targets:
+    - target: admission.k8s.gatekeeper.sh
+      rego: |
+        package aegis.suggestedaction
+
+        # params passed from Constraint parameters
+        params := input.parameters
+
+        # helper to safely fetch nested fields
+        get(obj, path) = val {
+          parts := split(path, ".")
+          val := reduce(parts, obj)
+        }
+
+        reduce(parts, cur) = res {
+          count(parts) == 0
+          res := cur
+        }
+        reduce(parts, cur) = res {
+          count(parts) > 0
+          head := parts[0]
+          tail := parts[1:]
+          next := cur[head]
+          res := reduce(tail, next)
+        }
+
+        # Only evaluate for SuggestedAction


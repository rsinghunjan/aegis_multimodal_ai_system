*** Begin Patch
*** Add File: .github/workflows/scheduled_smoke.yml
+name: Scheduled Smoke Tests (nightly / on-change)
+
+on:
+  schedule:
+    - cron: '0 3 * * *' # daily at 03:00 UTC
+  push:
+    branches:
+      - main
+  workflow_dispatch:
+
+permissions:
+  contents: read
+
+jobs:
+  smoke:
+    runs-on: ubuntu-latest
+    timeout-minutes: 60
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup
+        run: |
+          python -m pip install --upgrade pip
+          pip install requests || true
+          chmod +x scripts/*.sh || true
+
+      - name: Run minimal smoke suite
+        env:
+          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
+        run: |
+          ./scripts/smoke_checks.sh --quick --artifact-dir /tmp/aegis-smoke-${{ github.run_id }}
+
+      - name: Upload smoke artifacts
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: smoke-artifacts-${{ github.run_id }}
+          path: /tmp/aegis-smoke-${{ github.run_id


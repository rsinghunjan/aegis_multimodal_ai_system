 
objectStore:
  type: "s3"           # s3 | gcs | azure | oci | minio
  bucket: "aegis-staging-models"
  endpoint: ""         # set for S3-compatible endpoints or leave empty for cloud
database:
  url: "postgresql://aegis:password@db.example.com:5432/aegis"
ingress:

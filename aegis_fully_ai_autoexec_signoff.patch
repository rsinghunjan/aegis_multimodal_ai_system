*** Begin Patch
*** Add File: scripts/analyze_readiness_artifacts.py
+#!/usr/bin/env python3
+"""
+analyze_readiness_artifacts.py
+
+Lightweight analyzer for readiness gate artifacts.
+It can either:
+ - analyze a local artifacts tarball (.tgz/.tar.gz) produced by the readiness workflow, or
+ - download artifacts from a GitHub Actions workflow run id (requires GITHUB_TOKEN + GITHUB_REPOSITORY + WORKFLOW_RUN_ID).
+
+Usage examples:
+  # Analyze local tarball
+  python3 scripts/analyze_readiness_artifacts.py --artifact /tmp/aegis-validate.tgz
+
+  # Download artifacts from workflow run and analyze
+  GITHUB_TOKEN=... GITHUB_REPOSITORY=owner/repo WORKFLOW_RUN_ID=123456 python3 scripts/analyze_readiness_artifacts.py --download
+
+Outputs a short summary and a few suggested remediation lines.
+"""
+import argparse
+import json
+import os
+import re
+import sys
+import tarfile
+import tempfile
+from io import BytesIO
+
+import requests
+
+KEY_ERROR_PATTERNS = [
+    re.compile(r"Traceback", re.IGNORECASE),
+    re.compile(r"\bERROR\b", re.IGNORECASE),
+    re.compile(r"\bFAILED\b", re.IGNORECASE),
+    re.compile(r"Connection refused", re.IGNORECASE),
+    re.compile(r"timeout", re.IGNORECASE),
+    re.compile(r"authentication failed", re.IGNORECASE),
+    re.compile(r"permission denied", re.IGNORECASE),
+]
+
+SUGGESTION_MAP = [
+    (re.compile(r"milvus", re.IGNORECASE), "Milvus: check pods, TLS/credentials, and backup job logs."),
+   ](#)


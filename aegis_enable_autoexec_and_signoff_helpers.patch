*** Begin Patch
*** Add File: k8s/autoexec/enable-sandbox-autoexec.yaml
+apiVersion: v1
+kind: ConfigMap
+metadata:
+  name: aegis-autoexec
+  namespace: sandbox
+  labels:
+    managed-by: aegis-promotion-controller
+data:
+  enabled: "true"
+  enabled_by: "manual-promotion-pr"
+  enabled_at: "" # to be filled by CI / maintainer when applied
+
*** End Patch
*** Begin Patch
*** Add File: pr/enable_autoexec_sandbox.md
+Title: "[Autoexec] Enable Aegis auto-exec in sandbox namespace"
+
+Body:
+
+This PR enables Aegis automatic execution feature in the sandbox namespace after promotion checks passed.
+
+Files changed:
+- k8s/autoexec/enable-sandbox-autoexec.yaml (new ConfigMap)
+
+Checklist:
+- [ ] Verified readiness gate has passed for required stability window (N consecutive days)
+- [ ] Operator E2E rollback >= 99% across validation runs
+- [ ] Identity migration for operator/gateway validated in staging
+- [ ] Audit artifacts uploaded & retention verified
+- [ ] Security & SRE reviewers are assigned
+
+Notes:
+- If you did not expect this PR, please hold and review promotion controller logs before merging.
+
*** End Patch
*** Begin Patch
*** Add File: .github/workflows/create_signoff_from_latest_run_dispatch.yml
+name: Create Signoff Issue from Latest Successful Readiness Run (manual)
+
+on:
+  workflow_dispatch:
+    inputs:
+      workflow_name:
+        description: "Name of the readiness workflow (default is Fully AI Readiness Gate (Auto-evaluate))"
+        required: false
+        default: "Fully AI Readiness Gate (Auto-evaluate)"
+
+permissions:
+  contents: read
+  issues: write
+
+jobs:
+  create-signoff:
+    runs-on: ubuntu-latest
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup Python
+        uses: actions/setup-python@v4
+        with:
+          python-version: "3.10"
+
+      - name: Install dependencies
+        run: |
+          python -m pip install --upgrade pip
+          pip install requests || true
+
+      - name: Create prefilled signoff issue from latest successful run
+        env:
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+          GITHUB_REPOSITORY: ${{ github.repository }}
+        run: |
+          # This script was added earlier: scripts/create_prefilled_signoff_from_latest_artifacts.py
](#)


*** Begin Patch
*** Add File: .github/workflows/release_with_ops_gate.yml
+name: Release with Ops Validation Gate
+
+on:
+  workflow_dispatch:
+    inputs:
+      promote_tag:
+        description: 'Tag to promote (e.g. v1.2.3)'
+        required: true
+      env:
+        description: 'Target environment (staging|prod)'
+        required: true
+
+jobs:
+  ops-validation:
+    name: Run Ops Validation
+    runs-on: ubuntu-latest
+    outputs:
+      ops_status: ${{ steps.check.outputs.ops_status }}
+    steps:
+      - uses: actions/checkout@v4
+      - name: Install deps
+        run: |
+          sudo apt-get update && sudo apt-get install -y jq
+      - name: Run ops validation script
+        id: run
+        env:
+          PROM_URL: ${{ secrets.PROM_URL }}
+          OBJECT_STORE_BUCKET: ${{ secrets.OBJECT_STORE_BUCKET }}
+          VAULT_ADDR: ${{ secrets.VAULT_ADDR }}
+          VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}
+        run: |
+          chmod +x ./scripts/ops_run_operational_validation.sh || true
+          # The script should produce /tmp/ops_signoff_report.json
+          ./scripts/ops_run_operational_validation.sh --ci || true
+          if [ -f /tmp/ops_signoff_report.json ]; then
+            echo "ops_report_exists=true" > /tmp/ops_status
+          else
+            echo "ops_report_exists=false" > /tmp/ops_status
+          fi
+          cat /tmp/ops_status
+      - name: Upload ops report artifact
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: ops-signoff-report
+          path: /tmp/ops_signoff_report.json
+      - name: Check ops signoff JSON
+        id: check
+        run: |
+          if [ ! -f /tmp/ops_signoff_report.json ]; then
+            echo "::set-output name=ops_status::FAIL](#)


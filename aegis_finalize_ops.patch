*** Begin Patch
*** Add File: scripts/e2e_dispatch_and_report.sh
+#!/usr/bin/env bash
+#
+# Orchestrate an E2E run and produce a verification report.
+# - Optionally submit an Argo workflow (training) YAML.
+# - Wait for the workflow to complete.
+# - Download artifact evidence from S3 (artifact, sig, attestation, sbom).
+# - Run cosign verification (keyless or pubkey) and Rekor inclusion verification.
+# - Produce a JSON report with the verification results and logs for CI/attestation.
+#
+# Usage:
+#   OBJECT_STORE_BUCKET=... REKOR_URL=... ./scripts/e2e_dispatch_and_report.sh path/to/argo_workflow.yaml output_report.json
+set -euo pipefail
+
+WF_YAML="${1:-}"
+OUT_REPORT="${2:-e2e_report.json}"
+ARGO_NS="${ARGO_NS:-aegis}"
+BUCKET="${OBJECT_STORE_BUCKET:-}"
+REKOR_URL="${REKOR_URL:-}"
+TIMEOUT="${TIMEOUT:-900}"
+
+need() {
+  command -v "$1" >/dev/null 2>&1 || (echo "Missing required tool: $1" >&2; exit 2)
+}
+
+need kubectl
+need aws
+need jq
+
+report() {
+  jq -n \
+    --arg wf "${WF_NAME:-}" \
+    --arg artifact_key "${ART_KEY:-}" \
+    --arg rekor_uuid "${REKOR_UUID:-}" \
+    --arg cosign_ok "${COSIGN_OK:-unknown}" \
+    --arg rekor_ok "${REKOR_OK:-unknown}" \
+    '{workflow:$wf, artifact_key:$artifact_key, rekor_uuid:$rekor_uuid, cosign_ok:$cosign_ok, rekor_ok:$rekor_ok}'
+}
+
+if [ -z "$BUCKET" ]; then
+  echo "OBJECT_STORE_BUCKET must be set" >&2
+  exit 1
+fi
+
+if [ -n "$WF_YAML" ]; then
+  echo "Submitting Argo workflow: $WF_YAML"
+  kubectl apply -f "$WF_YAML" -n "$ARGO_NS"
+  sleep 5
+fi
+
+# discover latest workflow
+WF_NAME=$(kubectl get wf -n "$ARGO_NS" --sort-by=.metadata.creationTimestamp -o jsonpath='{.items[-1:].metadata.name}' 2>/dev/null || true)
+if [ -z "$WF_NAME" ]; then
+  echo "No workflow found in namespace $ARGO_NS" >&2
+  exit 3
+fi
+echo "Monitoring workflow: $WF_NAME (timeout ${TIMEOUT}s)"
+if ! kubectl wait --for=condition=Completed "workflow/$WF_NAME" -n "$ARGO_NS" --timeout="${TIMEOUT}s"; then
+  echo "](#)


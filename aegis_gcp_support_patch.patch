*** Begin Patch
*** Add File: scripts/ksa_to_gsa_bind.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# ksa_to_gsa_bind.sh
+# Bind a Kubernetes ServiceAccount (KSA) to a GCP Service Account (GSA) using
+# Workload Identity and verify the IAM binding was created.
+#
+# Usage:
+#   ./scripts/ksa_to_gsa_bind.sh --project PROJECT_ID --gsa EMAIL --k8s-namespace ns --ksa NAME
+#
+# Requires:
+# - gcloud CLI authenticated with permission to set IAM policy bindings on the project
+# - kubectl configured for the target cluster (or KUBE_CONFIG_DATA env containing base64 kubeconfig)
+
+usage() {
+  cat <<EOF
+Usage: $0 --project PROJECT_ID


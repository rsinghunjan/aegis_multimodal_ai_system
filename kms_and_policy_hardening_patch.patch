*** Begin Patch
*** Add File: .github/workflows/staged_kms_rotation.yml
+name: Staged KMS Rotation (staging)
+
+on:
+  workflow_dispatch:
+    inputs:
+      cloud:
+        description: 'cloud to rotate (aws|azure|gcp|oci)'
+        required: true
+        default: 'aws'
+      run_drill:
+        description: 'Run backup->verify drill after rotation (yes|no)'
+        required: false
+        default: 'yes'
+
+permissions:
+  contents: read
+  id-token: write
+
+jobs:
+  rotate-staging:
+    runs-on: ubuntu-latest
+    environment: dangerous-ops
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
+
+      - name: Setup kubeconfig (if provided)
+        run: |
+          if [ -n "${{ secrets.KUBE_CONFIG_DATA }}" ]; then
+            echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > /tmp/kubeconfig
+            mkdir -p $HOME/.kube
+            mv /tmp/kubeconfig $HOME/.kube/config
+          fi
+
+      - name: AWS: Configure credentials via OIDC when selected
+        if: ${{ github.event.inputs.cloud == 'aws' }}
+        uses: aws-actions/configure-aws-credentials@v2
+        with:
+          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
+          aws-region: ${{ secrets.AWS_REGION }}
+
+      - name: OCI / Azure / GCP auth notes
+        if: ${{ github.event.inputs.cloud != 'aws' }}
+        run: |
+          echo "Ensure appropriate auth is configured for cloud=${{ github.event.inputs.cloud }} (workload identity or service principal)."
+
+      - name: Pre-rotation: Trigger a staging backup (create Job from CronJob)
+        run: |
+          set -euo pipefail
+          echo "Triggering a pre-rotation backup to staging..."
+](#)


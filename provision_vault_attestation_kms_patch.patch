343
344
345
346
347
348
349
350
351
352
353
354
355
356
357
358
359
360
361
362
363
364
365
366
367
368
369
370
371
372
373
374
375
376
377
378
379
380
381
382
383
384
385
386
387
388
389
390
391
392
393
394
395
396
397
398
399
400
401
402
403
404
405
406
407
408
409
410
411
412
413
*** Begin Patch
+VAULT_ADDR=${VAULT_ADDR:-http://vault.ops.svc.cluster.local:8200}
+VAULT_TOKEN=${VAULT_TOKEN:-}
+SA_NAME=${1:-agent-controller-sa}
+SA_NS=${2:-staging}
+
+if [ -z "$VAULT_TOKEN" ]; then
+  echo "Set VAULT_TOKEN env var (bootstrapped admin token)"
+  exit 2
+fi
+
+export VAULT_ADDR
+vault login "$VAULT_TOKEN"
+vault auth enable kubernetes || true
+
+SA_SECRET=$(kubectl get sa "$SA_NAME" -n "$SA_NS" -o jsonpath='{.secrets[0].name}')
+TOKEN=$(kubectl get secret "$SA_SECRET" -n "$SA_NS" -o go-template='{{ index .data "token" }}' | base64 --decode)
+KUBERNETES_HOST=$(kubectl config view --raw --minify -o jsonpath='{.clusters[0].cluster.server}')
+CA_CERT=$(kubectl get secret "$SA_SECRET" -n "$SA_NS" -o go-template='{{ index .data "ca.crt" }}' | base64 --decode)
+
+vault write auth/kubernetes/config token_reviewer_jwt="$TOKEN" kubernetes_host="$KUBERNETES_HOST" kubernetes_ca_cert="$CA_CERT"
+
+vault policy write agent-role - <<EOF
+path "secret/data/agent/*" {
+  capabilities = ["read"]
+}
+EOF
+
+vault write auth/kubernetes/role/agent-role bound_service_account_names="$SA_NAME" bound_service_account_namespaces="$SA_NS" policies=agent-role ttl=1h
+echo "Vault Kubernetes auth configured for role agent-role bound to SA $SA_NAME/$SA_NS"
+
*** End Patch
*** Begin Patch
*** Add File: runbooks/execute_provision_attest_vault.md
+# Runbook: Provision KMS, Deploy Vault, Deploy Attestation & Gatekeeper, Run Tests
+
+Prereqs:
+- Terraform, kubectl, helm, gh, vault CLI, cosign, docker, jq installed locally.
+- GitHub CLI authenticated and GH_REPO env var set.
+- Kubernetes context pointed at staging cluster.
+
+Steps:
+1) Provision KMS & OIDC
+   - For AWS: edit terraform/aws/terraform.tfvars then:
+       cd terraform/aws && terraform init && terraform apply -auto-approve
+     Copy terraform output kms_key_arn and github_oidc_role_arn.
+   - Run: export GH_REPO="owner/repo"
+     ./scripts/set_github_secrets_aws.sh "<kms_key_arn>" "<rekor_url>" "<registry_host>" "<registry_user>" "<registry_token>" "<compliance_bucket>"
+
+2) Deploy Vault & migrate secrets
+   - helm upgrade --install aegis-vault hashicorp/vault -n ops -f helm/vault/values-prod.yaml
+   - Get bootstrap token (operator step). Then:
+       ./scripts/deploy_vault_and_migrate.sh <BOOTSTRAP_TOKEN>
+   - Configure apps to use Vault Agent/annotations (see k8s/samples/sample-app-vault.yaml)
+
+3) Deploy Attestation & Gatekeeper
+   - ./scripts/deploy_attestation_and_gatekeeper.sh
+   - Ensure attestation pods ready: kubectl get pods -n ops -l app=attestation
+   - Run attestation smoke test:
+       export REGISTRY=ghcr.io/yourorg
+       ./scripts/attestation_smoke_test.sh
+
+4) Validate
+   - Run tests/full_e2e_runner.sh to exercise load and scale tests.
+   - Run tests/pen_tests/attestation_and_sandbox_suite.sh and tests/agent_sandbox_pen_test.sh
+
+Notes:
+- Replace placeholders (registry, bucket, terraform vars) with real values.
+- For GCP, use terraform/gcp and adapt scripts/set_github_secrets_aws.sh or run manual GH secret creation.
+
*** End Patch
*** End Patch

*** Begin Patch
*** Add File: scripts/run_full_validation.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# Orchestrator script to run the full staging validation:
+# - deploy/verify Milvus (if script present)
+# - run Milvus stress test (if host provided)
+# - run LLM gateway load test (if URL provided)
+# - apply Gatekeeper policies for staging
+# - run operator E2E repeats
+# - run chaos tests
+# - collect KPIs baseline window (uses collect_kpis.py)
+# - evaluate KPIs against SLOs (uses evaluate_kpis.py)
+# - upload artifacts (if PUSH_ARTIFACT_DIR provided)
+# - optionally open a signoff issue if all checks pass (uses generate_signoff_issue.py)
+
+# Usage:
+#  ./scripts/run_full_validation.sh --minutes 60 --e2e-runs 50 --pushgateway http://pushgateway:9091 --create-signoff
+
+WORKDIR="$(cd "$(dirname "$0")" && pwd)/.."
+cd "$WORKDIR"
+
+PROM_URL="${PROM_URL:-}"
+PROM_TOKEN="${PROM_TOKEN:-}"
+PUSHGATEWAY_URL="${PUSHGATEWAY_URL:-}"
+KUBE_CONFIG_FILE="${KUBE_CONFIG_FILE:-/tmp/kubeconfig}"
+CREATE_SIGNOFF="${CREATE_SIGNOFF:-false}"
+MINUTES="${MINUTES:-60}"
+E2E_RUNS="${E2E_RUNS:-50}"
+ARTIFACT_DIR="${ARTIFACT_DIR:-/tmp/aegis-validation-artifacts-$(date +%s)}"
+
+print_help() {
+  cat <<EOF
+Usage: $0 [--minutes N] [--e2e-runs N] [--pushgateway URL] [--create-signoff] [--artifact-dir DIR]
+
+Environment:
+  PROM_URL, PROM_TOKEN            - Prometheus access
+  KUBE_CONFIG_FILE                - kubeconfig path to use (default /tmp/kubeconfig)
+  PUSHGATEWAY_URL                 - optional Pushgateway to push per-run metrics
+  GITHUB_TOKEN                    - required if --create-signoff to open issue
+
+EOF
+}
+
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --minutes) MINUTES="$2"; shift 2;;
+    --e2e-runs) E2E_RUNS="$2"; shift 2;;
+    --pushgateway) PUSHGATEWAY_URL="$2"; shift 2;;
+    --create-signoff) CREATE_SIGNOFF="true"; shift;;
+    --artifact-dir) ARTIFACT_DIR="$2"; shift 2;;
+    -h|--help) print_help; exit 0;;
+    *) echo "Unknown arg: $1"; print_help; exit 2;;
+  esac
+done
+
+mkdir -p "$ARTIFACT_DIR"
+echo "Artifacts will be stored in $ARTIFACT_DIR"
+
+export KUBECONFIG="${KUBE_CONFIG_FILE}"
+
+run_cmd_capture() {
+  # $1 = label, rest = command
+  label="$1"; shift
+  outfile="$ARTIFACT_DIR/${label}.log"
+  echo "=== START: $label ===" | tee "$outfile"
+  if "$@" >> "$outfile" 2>&1; then
+    echo "=== OK: $label ===" | tee -a "$outfile"
+  else
+    echo "=== FAILED: $label (see $outfile) ===" | tee -a "$outfile"
+    return 1
+  fi
+}
+
+echo "1) Milvus deploy & verify (if script present)"
+if [ -x scripts/deploy_milvus_verify.sh ]; then
+  run_cmd_capture "milvus_deploy_verify" ./scripts/deploy_milvus_verify.sh || true
+else
+  echo "deploy_milvus_verify.sh not present; skipping" > "$ARTIFACT_DIR/milvus_deploy_verify.log"
+fi
+
+echo "2) Milvus stress test (if host env present)"
+if [ -n "${MILVUS_HOST:-}" ] && [ -n "${MILVUS_PORT:-}" ] && [ -f scripts/milvus_stress_test.py ]; then
+  run_cmd_capture "milvus_stress" python3 scripts/milvus_stress_test.py --host "${MILVUS_HOST}" --port "${MILVUS_PORT}" --collection aegis_docs --inserts 500 --queries 200 --concurrency 8 || true
+else
+  echo "Milvus host/port or stress script missing; skipping" > "$ARTIFACT_DIR/milvus_stress.log"
+fi
+
+echo "3) LLM gateway load test (if URL provided)"
+if [ -n "${LLM_GATEWAY_URL:-}" ] && [ -f scripts/llm_load_test.py ]; then
+  run_cmd_capture "llm_load_test" python3 scripts/llm_load_test.py --url "${LLM_GATEWAY_URL}" --key "${LLM_GATEWAY_API_KEY:-}" --concurrency 10 --requests 300 || true
+else
+  echo "LLM gateway URL or load test script missing; skipping" > "$ARTIFACT_DIR/llm_load_test.log"
+fi
+
+echo "4) Apply Gatekeeper constraints (best-effort)"
+if kubectl get pods -n gatekeeper >/dev/null 2>&1; then
+  run_cmd_capture "apply_gatekeeper" kubectl apply -f k8s/gatekeeper_constrainttemplate_suggestedaction_v2.yaml || true
+  run_cmd_capture "apply_gatekeeper_constraints" kubectl apply -f k8s/constraint_require_approval_and_owner_v2.yaml || true
+else
+  echo "Gatekeeper not present in cluster; skipping gatekeeper apply" > "$ARTIFACT_DIR/gatekeeper_apply.log"
+fi
+
+echo "5) Operator E2E repeats (collect metrics to Pushgateway if available)"
+if [ -f scripts/operator_e2e_repeat.py ]; then
+  export E2E_RUNS="$E2E_RUNS"
+  # Run in smaller batches to allow exporter pushes per-batch
+  batch=20
+  remaining=$E2E_RUNS
+  instance_prefix="validation-run-$(date +%s)"
+  while [ "$remaining" -gt 0 ]; do
+    runs=$(( remaining < batch ? remaining : batch ))
+    echo "Running $runs E2E cycles (remaining $remaining)"
+    # operator_e2e_repeat.py should optionally accept runs via env E2E_RUNS
+    E2E_RUNS="$runs" python3 scripts/operator_e2e_repeat.py*](#)


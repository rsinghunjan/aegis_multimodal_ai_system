*** Begin Patch
*** Add File: scripts/e2e_staging_runner.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# End-to-end staging runner:
+# - verifies operator auto-exec is OFF
+# - runs Milvus deploy/verify, LLM upstream deploy, gateway load test
+# - runs PR Assistant E2E (with staging PR or local simulation)
+# - runs adversarial + fuzz tests
+# - runs a chaos test (kill a Milvus pod) and verifies recovery
+# - runs KPI reporter and fails if KPIs breach thresholds
+# - uploads artifacts for SRE review
+#
+# Expected environment variables (CI should provide):
+# - KUBE_CONFIG_DATA (base64) OR kubeconfig available locally
+# - LLM_GATEWAY_URL, LLM_GATEWAY_API_KEY (for load test)
+# - MILVUS_HOST / MILVUS_PORT for direct checks (optional)
+# - PROM_URL, PROM_TOKEN (optional)
+# - AUDIT_GCS_BUCKET / AUDIT_S3_BUCKET for audit verification (optional)
+
+WORKDIR="$(cd "$(dirname "$0")/.." && pwd)"
+export PATH="$PATH:/usr/local/bin"
+
+if [ -n "${KUBE_CONFIG_DATA:-}" ]; then
+  echo "$KUBE_CONFIG_DATA" | base64 --decode > /tmp/kubeconfig
+  export KUBECONFIG=/tmp/kubeconfig
+fi
+
+echo "1) Ensure operator auto-execute is OFF"
+scripts/check_operator_exec_flag.sh
+
+echo "2) Deploy & verify Milvus (uses existing workflow/script)"
+if [ -f ".github/workflows/milvus_deploy_verify_full.yml" ]; then
+  echo "Triggering milvus_deploy_verify_full via local script if present..."
](#)


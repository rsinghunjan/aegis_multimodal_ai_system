*** Begin Patch
*** Add File: .github/workflows/milvus_deploy_verify_full.yml
+name: Milvus Full Deploy, HA Verification & Backup Check
+
+on:
+  workflow_dispatch:
+
+jobs:
+  deploy_and_verify:
+    runs-on: ubuntu-latest
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup kubectl & helm
+        run: |
+          sudo apt-get update
+          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
+          chmod +x kubectl && sudo mv kubectl /usr/local/bin/
+          curl -fsSL https://get.helm.sh/helm-v3.12.0-linux-amd64.tar.gz | tar xz
+          sudo mv linux-amd64/helm /usr/local/bin/helm
+
+      - name: Configure KUBECONFIG
+        env:
+          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
+        run: |
+          echo "$KUBE_CONFIG_DATA" | base64 --decode > /tmp/kubeconfig
+          export KUBECONFIG=/tmp/kubeconfig
+          kubectl version --client
+
+      - name: Deploy Milvus via Helm (prod values)
+        env:
+          MILVUS_VALUES_BASE64: ${{ secrets.MILVUS_VALUES_BASE64 }}
+        run: |
+          helm repo add milvus https://milvus-io.github.io/milvus-helm/ || true
+          helm repo update
+          if [ -n "$MILVUS_VALUES_BASE64" ]; then
+            echo "$MILVUS_VALUES_BASE64" | base64 --decode > /tmp/milvus-values.yaml
+            helm upgrade --install aegis-milvus milvus/milvus -n aegis -f /tmp/milvus-values.yaml --create-namespace
+          else
+            helm upgrade --install aegis-milvus milvus/milvus -n aegis --create-namespace
+          fi
+
+      - name: Wait for pods ready
+        run: |
+          kubectl -n aegis wait --for=condition=ready pods --all --timeout=15m
+
+      - name: Verify TLS secret & root auth secret
+        run: |
+          kubectl -n aegis get secret aegis-milvus-tls || (echo "TLS secret missing" && exit 2)
+          kubectl -n aegis get secret aegis-milvus-root || (echo "root auth secret missing" && exit 2)
+
+      - name: Run basic Milvus health check (port-forward best-effort)
+        run: |
+          POD=$(kubectl -n aegis get pods -l app.kubernetes.io/name=milvus -o jsonpath='{.items[0].metadata.name}')
+          if [ -z "$POD" ]; then echo "No milvus pod found"; exit 2; fi
+          kubectl -n aegis port-forward "$POD" 19121:19121 >/tmp/milvus-pf.log 2>&1 &
+          PF=$!
+          sleep 3
+          curl -sS http://127.0.0.1:19121/ || (kubectl -n aegis logs "$POD" --tail=50 && kill $PF && exit 3)
+          kill $PF
+
+      - name: Trigger one-off backup & verify artifact presence (best-effort)
+        env:
+          GCS_BUCKET: ${{ secrets.MILVUS_BACKUP_GCS_BUCKET }}
+          S3_BUCKET: ${{ secrets.MILVUS_BACKUP_S3_BUCKET }}
+        run: |
+          CRON=$(kubectl -n aegis get cronjob -o jsonpath='{.items[?(@.metadata.name=="milvus-backup")].metadata.name}' || true)
+          if [ -n "$CRON" ]; then
+            JOB_JSON=$(kubectl -n aegis create job --from=cronjob/"$CRON" "manual-backup-$(date +%s)" -o json)
+            JOB_NAME=$(echo "$JOB_JSON" | jq -r .metadata.name)
+            kubectl -n aegis wait --for=condition=complete job/"$JOB_NAME" --timeout=5m || echo "Backup job may not complete"
+            kubectl -n aegis logs job/"$JOB_NAME" || true
+            # attempt to list artifacts if configured
+            if [ -n "$GCS_BUCKET](#)
    

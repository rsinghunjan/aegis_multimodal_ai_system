*** Begin Patch
*** Add File: scripts/run_full_staging_proof.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# Orchestrator to run the Full Staging Proof locally or in CI.
+# It runs the readiness gate, operator-scale proof, upstream load tests,
+# Milvus HA/backup/failover checks, identity/secret migration guidance,
+# audit enable/verify, KPI baseline collection and final signoff creation.
+#
+# Usage:
+#   KUBE_CONFIG_DATA=... PROM_URL=... PROM_TOKEN=... PUSHGATEWAY_URL=... \
+#     LLM_GATEWAY_URL=... LLM_GATEWAY_API_KEY=... GITHUB_TOKEN=... \
+#     ./scripts/run_full_staging_proof.sh --e2e-runs 500 --kpi-days 3 --artifact-dir /tmp/aegis-full-$(date +%s)
+#
+E2E_RUNS=500
+KPI_DAYS=3
+ARTIFACT_DIR="/tmp/aegis-full-$(date +%s)"
+
+print_help() {
+  cat <<EOF
+Usage: $0 [--e2e-runs N] [--kpi-days N] [--artifact-dir DIR]
+
+Environment variables expected (set as needed):
+  KUBE_CONFIG_DATA (base64 kubeconfig) - optional
+  PROM_URL, PROM_TOKEN
+  PUSHGATEWAY_URL (optional)
+  LLM_GATEWAY_URL, LLM_GATEWAY_API_KEY (optional)
+  GITHUB_TOKEN (optional, for creating signoff/triage issues)
+  CLOUD_PROVIDER (gcp|aws) and corresponding bucket envs (GCS_AUDIT_BUCKET / S3_AUDIT_BUCKET)
+EOF
+}
+
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --e2e-runs) E2E_RUNS="$2"; shift 2;;
+    --kpi-days) KPI_DAYS="$2"; shift 2;;
+    --artifact-dir) ARTIFACT_DIR="$2"; shift 2;;
+    -h|--help) print_help; exit 0;;
+    *) echo "Unknown arg: $1"; print_help; exit 2;;
+  esac
+done
+
+mkdir -p "$ARTIFACT_DIR"
+
+# Decode kubeconfig if provided
+if [ -n "${KUBE_CONFIG_DATA:-}" ]; then
+  KUBECONFIG_FILE="${ARTIFACT_DIR}/kubeconfig"
+  echo "$KUBE_CONFIG_DATA" | base64 --decode > "$KUBECONFIG_FILE"
+  export*](#)


*** Begin Patch
*** Add File: scripts/configure_and_lock_gcs_retention_with_approval.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# configure_and_lock_gcs_retention_with_approval.sh
+# Safely configure GCS retention and optionally lock it after explicit approval.
+#
+# IMPORTANT: Locking retention is irreversible. This script provides a manual approval
+# step (interactive or via GH Issue link) and a dry-run mode. Use --confirm-lock to
+# perform irreversible lock; otherwise it only sets retention.
+#
+# Usage:
+#   ./scripts/configure_and_lock_gcs_retention_with_approval.sh --bucket BUCKET --days 30 [--lock] [--dry-run] [--approve-token TOKEN]
+#
+BUCKET=""
+DAYS=0
+LOCK=false
+DRY_RUN=false
+APPROVE_TOKEN=""
+
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --bucket) BUCKET="$2"; shift 2;;
+    --days) DAYS="$2"; shift 2;;
+    --lock) LOCK=true; shift;;
+    --dry-run) DRY_RUN=true; shift;;
+    --approve-token) APPROVE_TOKEN="$2"; shift 2;;
+    -h|--help) echo "Usage: $0 --bucket BUCKET --days N [--lock] [--dry-run] [--approve-token TOKEN]"; exit 0;;
+    *) echo "Unknown arg: $1"; exit 2;;
+  esac
+done
+
+if [ -z "$BUCKET" ] || [ "$DAYS" -le 0 ]; then
+  echo "Missing required args. See --help"
+  exit 2
+fi
+
+echo "Setting retention policy: ${DAYS}d on gs://${BUCKET}"
+if [ "$DRY_RUN" = true ]; then
+  echo "[DRY-RUN] gsutil retention set ${DAYS}d gs://${BUCKET}"
+else
+  gsutil retention set "${DAYS}d" "gs://${BU*


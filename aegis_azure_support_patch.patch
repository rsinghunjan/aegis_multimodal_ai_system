*** Begin Patch
*** Add File: scripts/azure_create_uami_and_bind.sh
+#!/usr/bin/env bash
+set -euo pipefail
+#
+# azure_create_uami_and_bind.sh
+# Create a User Assigned Managed Identity (UAMI), assign Storage role, and emit
+# the Kubernetes CR examples for aad-pod-identity (v1) and pod-identity v2 adapters.
+#
+# Usage:
+#   ./scripts/azure_create_uami_and_bind.sh --subscription SUB --resource-group RG \
+#     --name uami-aegis-operator --namespace aegis --ksa aegis-operator \
+#     --storage-account <storage-account> --role "Storage Blob Data Contributor"
+#
+# Requirements:
+# - az CLI authenticated with permissions to create identities and role assignments
+# - kubectl configured for the target AKS cluster (or set KUBE_CONFIG_DATA env)
+#
+usage() {
+  cat <<EOF
+Usage: $0 --subscription SUB --resource-group RG --name UAMI_NAME --namespace K8S_NS --ksa KSA_NAME --storage-account STORAGE_ACCOUNT [--role ROLE] [--dry-run]
+
+This will:
+ - create a user assigned managed identity (UAMI)
+ - assign the requested role (default: "Storage Blob Data Reader") scoped to storage account
+ - print kubectl YAML for aad-pod-identity (AzureIdentity / AzureIdentityBinding) and pod-identity v2 examples
+EOF
+}
+
+SUBSCRIPTION=""
+RG=""
+NAME=""
+K8S_NS=""
+KSA=""
+STORAGE_ACCOUNT=""
+ROLE="Storage Blob Data Reader"
+DRY_RUN="false"
+
+while [ $# -gt 0 ]; do
+  case "$1" in
+    --subscription) SUBSCRIPTION="$2"; shift 2;;
+    --resource-group) RG="$2"; shift 2;;
+    --name) NAME="$2"; shift 2;;
+    --namespace) K8S_NS="$2"; shift 2;;
+    --ksa) KSA="$2"; shift 2;;
+    --storage-account) STORAGE_ACCOUNT="$2"; shift 2;;
+    --role) ROLE="$2"; shift 2;;
+    --dry-run) DRY_RUN="true"; shift;;
+    -h|--help) usage; exit 0;;
+    *) echo "Unknown arg: $1"; usage; exit 2;;
+  esac
+done
+
+if [ -z "$SUBSCRIPTION" ] || [ -z "$RG" ] || [ -z "$NAME" ] || [ -z "$K8S_NS" ] || [ -z "$KSA" ] || [ -z "$STORAGE_ACCOUNT" ]; then
+  usage
+  exit 2
+fi
+
+echo "Subscription: $SUBSCRIPTION"
+echo "Resource group: $RG"
+echo "UAMI name: $NAME"
+echo "K8s namespace: $K*](#)


*** Begin Patch
*** Add File: .github/workflows/gcp_oidc_auth_example.yml
+name: GCP OIDC Auth Example
+
+on:
+  workflow_dispatch:
+    inputs:
+      run_command:
+        description: "Command to run once GCP auth is configured"
+        required: false
+        default: "gcloud auth list && gcloud projects list --limit 5"
+
+permissions:
+  id-token: write
+  contents: read
+
+jobs:
+  gcp-oidc:
+    runs-on: ubuntu-latest
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Authenticate to GCP using Workload Identity Federation
+        uses: google-github-actions/auth@v1
+        with:
+          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
+          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_EMAIL }}
+
+      - name: Verify identity
+        run: |
+          echo "Running: ${{ github.event.inputs.run_command }}"
+          ${{ github.event.inputs.run_command }}
+
+      - name: Example: list GCS buckets (dry-run)
+        run: |
+          gsutil ls -p "${{ secrets.GCP_PROJECT_ID }}" || true
+
+# Notes:
+# - Create a Workload Identity Provider in GCP that trusts GitHub Actions OIDC tokens.
+# - Set GCP_WORKLOAD_IDENTITY_PROVIDER to the provider resource name (projects/.../locations/global/workloadIdentityPools/.../providers/...)
+# - Set GCP_SERVICE_ACCOUNT_EMAIL to the service account email to impersonate.
+
*** End Patch
*** Begin Patch
*** Add File: .github/workflows/weekly_git_history_scan.yml
+name: Weekly Git History Secret Scan
+
+on:
+  schedule:
+    - cron: '0 4 * * 1'   # weekly on Monday at 04:00 UTC
+  workflow_dispatch:
+
+permissions:
+  contents: read
+
+jobs:
+  history-scan:
+    runs-on: ubuntu-latest
+*

